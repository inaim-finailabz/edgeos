# Benchmarks
Every number in the main README must be reproducible from a script in this folder.
Record: device, OS/driver versions, llama.cpp commit, model + quant, prompt length, tok/s (prompt eval and generation separately).
